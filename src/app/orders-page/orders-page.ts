export interface Orders {
    id: Number,
    email: String,
    price: Number,
    quantity: Number,
    address: String
}
// If needed
export interface OrdersDTO {
    id: Number,
    email: String,
    price: Number,
    quantity: Number,
    address: String
}